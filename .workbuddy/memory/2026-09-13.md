# 2026-09-13

## 知乎黑客松项目「观点进化缸」（超级生态）

- 完成可行性审查：逐项对照官方 API 能力，结论为"核心概念成立、数据根基需重构"（无问题-回答列表接口、无历史时序、无折叠/粉丝/收藏数据）。产出《docs/观点进化缸-项目文档.md》。
- 独立完成全栈项目 `app/`（生产级 v1）：
  - server/：Express + 知乎开放平台客户端（Bearer + 时间戳鉴权）、三级缓存（热榜 1h / 生态缸 24h / 直答守卫 90 次/日）、直答 fast 批量标注 + thinking 放生分析、JSON 健壮解析（括号配平 extractJson）、透明规则概率模型（P = 0.35×蓝海 + 0.25×立场差异 + 0.2×权威缓冲 + 0.2×互动，与 AI 按 6:4 混合）、演示数据降级模式。
  - web/：React18+TS+Vite+d3-force（同步 300 tick 布局）；四大面板（演化时间轴/环境干预/放生实验/生态报告）；自绘 SVG 报告 → PNG 导出（同源无污染）。
  - 端到端验证通过：/api/status /api/hot /api/ecosystem /api/release 全 200；SPA 静态托管验证通过（tsc 零错误，gzip 60KB）。
- 文档三件套：README（架构+快速开始+诚信声明）、docs/DEVELOPMENT.md（开发实录+踩坑）、docs/SUBMISSION.md（合规自查清单）。

## 环境踩坑（重要，复用价值高）

- 本机 PowerShell 工具对原生命令 stdout 经常吞输出 → 一律落盘再 Read（node 子进程 + writeFileSync 最可靠）。
- PowerShell 工具禁止 cmd.exe，npm.cmd 被拦截 → 用 `node.exe node_modules/npm/bin/npm-cli.js` 直调。
- 默认 npm registry 超时（ETIMEDOUT 280s）→ `--registry=https://registry.npmmirror.com` + run_in_background。
- PowerShell `*>` 重定向会往日志里混入 NativeCommandError 包装文本 → 诊断收集用 node 子进程文件 IO。
- `tsc -b` 严格模式：d3-force 的 `SimulationLinkDatum.source` 是 `string|number|Node` 联合，渲染前需 typeof 收窄。
- 服务端 8787 正在后台运行（演示模式），用户可直接打开 http://localhost:8787 查看。

## 深夜重大更新（21:30-22:30）：Access Secret 接入 + 隐藏接口发现

- 用户提供 Access Secret，已写入 `app/server/.env`（gitignore 已覆盖，不入库）。服务切换实时模式。
- **实测额度与文档严重不符**：直答 2/日（非100）、热榜 2/日（非100）、搜索 10/日、问题回答 10/日、用户数据 1000/日。429 不消耗额度，200+解析失败会消耗。
- **发现隐藏端点 `/api/v1/content/question_answers`**：文档未记载；参数 `QuestionUrl`（完整知乎问题链接），返回该问题下真实回答（ContentToken/Url/Summary + Paging{IsEnd,NextOffset}），无赞同数字段。方法论：quota 清单是隐藏能力地图，错误信息会提示正确参数名。
- 数据管线升级为双通道：A) URL 直取 + 知乎搜索 LCS≥20 汉字匹配富化赞同数（搜索摘要为查询偏置中段片段，前缀匹配 0 命中的实测教训）；B) 仅标题时搜索汇聚。热榜耗尽返回空列表，需显式判空降级演示热榜+警告横幅。
- 端到端验证通过：通道 A 10 物种直取、785 赞同精确富化、直答耗尽时标注/放生自动降级规则模型（annSrc=none、rule-only）。
- 文档同步更新：可行性审查结论（缺口1已闭合）、README 双通道+实测额度表、DEVELOPMENT.md 新增第 0 节方法论、SUBMISSION.md 指标修正。
- 热榜/直答今日额度已耗尽，明日 0 点重置；演示日预案：上午预构建 1-2 个演示缸（缓存 24h）。
- 服务实例运行于 8787（后台任务 j26xub）；本机多次重启注意先 TaskStop 旧实例避免 EADDRINUSE。

## 缓存加固（22:15）

- 用户要求防止同一请求重复消耗额度。实现"额度防泄漏五道闸"：结果缓存（热榜12h/缸24h/搜索6h/qa6h/放生1h）、并发去重 dedup()（3 并发实测仅 1 次上游构建）、生态缸缓存磁盘持久化（server/data/eco-cache.json，重启不丢）、双层直答守卫、stale-while-error。全部实测通过（复请求 cached:true、放生二次提交命中缓存、磁盘文件生成）。
- 服务实例 d88aet 运行于 8787。

## UI 全面改版（22:22）

- 设计方向：生态缸本体改为深蓝沉浸式画布（#0D1B2E + 微点阵 pattern + 半透明描边物种节点 + HUD 玻璃芯片 + 缸底图例条），外层面板保持浅色编辑风；选题台改为大标题 hero + 编辑式排行榜（大号序号数字、左侧蓝色 hover 竖线）；右侧面板改 segmented 分段控件；放生报告用 3/4 圆弧仪表盘。
- 新增 STANCE_DARK 深色画布配色表（types.ts）；构建曾因 report.ts null votes 类型错误静默失败（tsc 失败但 Test-Path dist 仍 True——验证 UI 改版必须比对 bundle 哈希而非仅查文件存在）。
- 冒烟验证通过：新 bundle h22b611d、hero/tank-svg-wrap/segmented 类均生效；ecoCacheCount=3 证明磁盘持久化跨重启生效。
- 服务实例 d88aet 运行于 8787。

## 待办/下一步

- ~~配置 Access Secret~~（已完成）
- 明日额度重置后：预构建演示缸 + 直答标注验证（zhida-fast 标注质量从未实测，需检查 stance/strategy 分布）
- 部署上线（Vercel 或 Lighthouse SSH）→ 完成提交（截止 9-15 10:00）
- V2 候选：OAuth「我的观点画像」、定时快照真实纵向时间轴、global_search 扩样本
